# قانون تولید محتوای آموزشی

## جریان تأیید

1. هیچ ویدیوی آموزشی بدون تأیید صاحب کانال رندر یا ارسال نمی‌شود.
2. هر روز ساعت ۰۸:۰۰ اروپا/برلین، بات فقط موضوع‌های پیشنهادی فردا را می‌فرستد؛ دوشنبه‌ها برنامهٔ هفته هم فرستاده می‌شود.
3. انتخاب‌ها باید از سه منبع ساخته شوند: جستجوی واقعی کاربران، ترند روز یا هفته، و آپدیت رسمی پلتفرم. نتیجهٔ جستجو به‌همراه لینک برای بررسی فرستاده می‌شود.
4. برای ساخت، کاربر فقط `تأیید <شناسهٔ موضوع>` می‌فرستد؛ نمونه: `تأیید trial-reels`.
5. تا رسیدن این تأیید، بات فقط برنامه و لینک‌ها را می‌فرستد و MP4 نمی‌سازد.

## برنامهٔ روزانه

- روزانه سه موضوع مستقل انتخاب می‌شود: یک موضوع برای TikTok، یک موضوع برای Instagram و یک موضوع کاربردیِ AI/مهارت/کسب‌وکار.
- موضوع سوم یک‌بار تحقیق و نوشته می‌شود، اما در دو بسته‌بندی بومی خروجی می‌گیرد: `ai-tiktok` و `ai-instagram`. پس برنامه سه ایده و چهار فایل ویدیویی می‌سازد؛ این دو فایل «ایدهٔ تکراری» نیستند.
- موضوع لازم نیست دربارهٔ خود TikTok یا Instagram باشد. هر موضوع فقط وقتی وارد برنامه می‌شود که تقاضای جست‌وجویی، مدرک قابل‌نمایش، اقدام عملی و پیوند روشن با مهارت، مشتری یا درآمدِ ممکن داشته باشد.
- خبرِ لحظه‌ای فقط وقتی انتخاب می‌شود که منبع اول معتبر و تاریخ‌دار باشد و بتوان آن را پیش از کهنه‌شدن ساخت؛ در غیر این صورت محتوای جست‌وجومحورِ ماندگار اولویت دارد.

## استاندارد محتوا

- اول منفعت واقعی مخاطب: بیشتر دیده‌شدن، آزمایش محتوا برای مخاطب تازه، فروش یا درآمدِ ممکن؛ هرگز View، Viral یا درآمد را تضمین نکن.
- قلاب در سه ثانیهٔ اول یک جملهٔ کوتاه و مخاطب‌محور است: پیش‌فرض، یک ادعای مستقیمِ قابل‌وفا یا تضاد شخصی است؛ سؤال فقط وقتی استفاده می‌شود که از ادعا قوی‌تر و طبیعی‌تر باشد. جواب یا مسیر در همان اسلاید نوشته نمی‌شود؛ پایین آن فقط یک حلقهٔ باز مانند «مرحلهٔ ۴م را از دست نده» می‌آید.
- واژه‌های شناخته‌شده در کنار فارسی یا به انگلیسی می‌آیند: `View`، `Reel`، `Edit`، `Save`، `Search`، `Share`.
- برای اپ‌های رایگان، «رایگان» باید واضح گفته شود؛ برای قابلیت‌های پلتفرم، رایگان بودن را بدون ادعای بی‌اساس ننویس.
- پایان ویدیو یک سؤال مرتبط برای کامنت است، نه درخواست Like یا Follow. درخواست «یک کلمه در کامنت بنویس» فقط وقتی مجاز است که در برابرش یک لینک، قالب یا راهنمای واقعی آمادهٔ ارسال باشد.
- عنوان تلگرام و هشتگ‌ها انگلیسی‌اند و `#viral` الزامی است.

## آمادگی برای انتشار و یادگیری

- پیش‌نویس پیش از تأیید، با امتیاز «آمادگی محتوا» بررسی می‌شود: قلابِ کوتاه و روشن، منفعت واقعی، حداقل دو گام قابل‌اجرا، CTA گفت‌وگوساز و منبعِ ادعاهای زمان‌حساس.
- هر موضوع در سه نسخه آزمایش می‌شود: قلاب/فریم اول متفاوت، مدرک بصری متفاوت و CTA متفاوت. فقط یک متغیر اصلی در هر نسخه عوض شود تا نتیجه قابل‌فهم بماند.
- پس از انتشار، ۲ و ۶ ثانیهٔ اول، میانگین زمان تماشا، نرخ تکمیل، ذخیره/اشتراک‌گذاری و کامنت بررسی می‌شود. نتیجه برای بهترشدن نسخهٔ بعدی است، نه اثبات «الگوریتم» یا تضمین View.

## استاندارد طراحی و صدا

- هر اسلاید، تصویرِ همان مرحله را دارد؛ متنِ Save فقط Bookmark، متنِ Share فقط آیکن یا صفحهٔ Share، و متنِ Analytics فقط نمودار یا صفحهٔ Insights.
- در آموزش اپ‌ها، قاب iPhone و نام/مسیر واقعی انگلیسی آن صفحه نمایش داده می‌شود. بازسازی رابط باید به‌عنوان «اسکرین‌شات واقعی» معرفی نشود.
- قلاب باید لوگوی واقعی و بزرگ پلتفرم یا اپِ موضوع را نشان دهد؛ لوگوی AfghanFollowers جای آن استفاده نمی‌شود.
- سه اسلاید یک ویدیو و ویدیوهای یک روز نباید قالب تکراری داشته باشند. رنگ، نوع حرکت، صفحهٔ iPhone، تصویر، گذار و موسیقی باید از متن همان موضوع مشتق شوند.
- موسیقی برای هر ویدیو جدا ساخته می‌شود؛ ضرب و افکت تغییر اسلاید با عمل همان اسلاید هماهنگ است، شاد و پرانرژی است و بر متن غلبه نمی‌کند.
