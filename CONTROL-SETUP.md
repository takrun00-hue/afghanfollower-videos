# کنترل از گوشی — دو روش

## روش ۱) ربات تلگرام (کنترل با گوشی، کامپیوتر باید روشن باشد)

یک ربات که روی کامپیوتر گوش می‌دهد و از گوشی با پیام کنترلش می‌کنی.

**شروع فوری:** فایل `start-bot.cmd` را در پوشهٔ پروژه دوبار کلیک کن.

**اجرای خودکار با هر لاگین:** از قبل فعال شده — یک فایل `afghanfollower-bot.vbs` در پوشهٔ Startup ویندوز گذاشته شده که ربات را مخفیانه اجرا می‌کند. برای غیرفعال‌کردن، آن فایل را از این مسیر پاک کن:
`C:\Users\mohse\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\`

**دستورها (از گوشی، در چت ربات):**
- `بساز` → ساخت و ارسال ۴ ویدیوی امروز
- `بفرست` → ارسال دوبارهٔ ویدیوهای امروز
- `وضعیت` → وضعیت امروز
- `راهنما` → فهرست دستورها

> ربات فقط به چت خودت جواب می‌دهد. کامپیوتر باید روشن و آنلاین باشد.

---

## روش ۲) اجرای ابری با GitHub Actions (بدون نیاز به کامپیوتر)

هر روز خودکار در فضای ابری اجرا می‌شود و به تلگرام می‌فرستد — کامپیوترت لازم نیست روشن باشد.
از دکمهٔ «Run workflow» در اپ گوشی گیت‌هاب هم می‌توانی دستی اجرا کنی.

**مراحل (یک‌بار):**

1. گیت را نصب کن: <https://git-scm.com/download/win>
2. در پوشهٔ پروژه، مخزن بساز و آپلود کن (توکن در `.env` آپلود نمی‌شود چون در `.gitignore` است):
   ```bash
   cd "D:\ClaudeFile\Create Videos\videos\tiktok-viral-tips"
   git init && git add . && git commit -m "AfghanFollower daily videos"
   ```
3. در GitHub یک مخزن **Private** بساز و طبق راهنمای GitHub، `git remote add origin ...` و `git push -u origin main` کن.
4. در صفحهٔ مخزن: **Settings → Secrets and variables → Actions → New repository secret** و این دو را اضافه کن:
   - `TELEGRAM_BOT_TOKEN` = توکن رباتت
   - `TELEGRAM_CHAT_ID` = `7993801735`
5. تمام. از **Actions → AfghanFollower Daily Videos → Run workflow** می‌توانی همین حالا (از گوشی) تست کنی.
   اجرای خودکار هر روز ساعت **۰۸:۰۰ کابل** انجام می‌شود.

**نکات:**
- زمان اجرای زمان‌بند گیت‌هاب گاهی چند دقیقه تأخیر دارد (طبیعی است).
- فایل موزیک (`music/bed-60s-v*.m4a`) و فونت‌ها و لوگو در مخزن هستند و لازم‌اند؛ آن‌ها را حذف نکن.
- `.env` هرگز آپلود نمی‌شود؛ توکن فقط به‌صورت Secret در گیت‌هاب ذخیره می‌شود.
