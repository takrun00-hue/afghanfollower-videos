# PROJECT_LAWS — استخراج وضعیت پروژه

تاریخ: ۲۰۲۶-۰۹-۱۳ — وضعیت: In progress، استخراج اولیهٔ مستند.

این فایل خلاصهٔ شواهد است، نه قانون‌نامهٔ موازی. `PROJECT_RULES.md` و اصلاحات تاریخ‌دار آن مرجع اصلی‌اند. درخواست جدید کاربر باید صریحاً نسبت به آن ثبت شود. ادعای خواندن کامل تمام فایل‌ها یا تأیید وضعیت محیط ابری نداریم: قوانین اصلی، پیکربندی‌ها و مسیرهای کلیدی بررسی شده‌اند؛ بانک‌های بزرگ محتوا، تمام Worker و تمام Markdownهای قدیمی هنوز ممیزی کامل نشده‌اند. پیش از پذیرش نهایی معماری، این پوشش باید تکمیل شود. کلیدهای خصوصی، باینری‌ها و خروجی‌های تولیدی متن قانون محسوب نمی‌شوند و محتوای اسرار نباید خوانده یا منتشر شود.

## GOALS

G01. تولید محتوای آموزشی فارسی برای مخاطب فارسی‌زبان، با توجه به مخاطب افغانستان؛ هدف منفعت عملی، یادگیری، صرفه‌جویی، رشد یا امکان درآمد مستند است، نه تضمین درآمد. شواهد: قوانین ۶–۱۴، `lib/selection-gate.mjs`.

G02. GapMedia فقط دربارهٔ TikTok/Instagram نیست؛ اپ، سرویس، ایده و مهارت کاربردی می‌تواند موضوع باشد. این دو شبکه مقصد انتشارند. شواهد: قوانین ۴–۵، `daily.yml`.

G03. مسیر جداگانهٔ آموزش A1 آلمانی برای فارسی‌زبانان، با واژه، معنی، مثال و تلفظ آلمانی مستقل. برند فعلی «آموزش آلمانی هوشمند» است؛ German Insider در پیوست پنجم کنار گذاشته شده. شواهد: `lib/brand.mjs`، `german-lesson-build.mjs`، `lib/german-a1.mjs`.

G04. کار بدون روشن‌بودن کامپیوتر، دریافت فرمان تلگرام و تولید زمان‌بندی‌شده؛ فقط خروجیِ گذرانده از کنترل کیفیت ارسال شود. شواهد: Worker و Actions، قوانین ۱۵–۲۰ و ۹۵–۱۰۰.

G05. جلوگیری از محتوای تکراری پیش از هزینهٔ تولید؛ ادیت و خروجیِ فرمت دوم همان موضوع، تکرار ناخواسته نیستند. شواهد: قانون ۷۲-ک، `lib/dedupe.mjs` و مسیر correction درس.

## RULES

R01. قانون مطلوب، رفتار کد و ادعای کامنت سه چیز جدا هستند. سبزبودن یک تست معادل شنیدن نریشن یا اثبات کیفیت گرافیک نیست.

R02. زمان آموزش GapMedia: ۰۸:۳۰ و ۱۷:۰۰ در `Europe/Berlin`، هر نوبت دو نسخهٔ بومی. کد فعلی هر ۱۵ دقیقه گیت زمان را بررسی می‌کند؛ شروع دقیق رأس ساعت با این پیاده‌سازی تضمین نیست. `daily.yml`.

R03. خروجی معمول ۱۰۸۰×۱۹۲۰؛ 4K با درخواست. طول نهایی باید از مدت صدای واقعی مشتق شود، نه کوتاه‌کردن اجباری جمله. `PROJECT_RULES.md` §۱۲، `daily-render.mjs` و `music/plan-voice.mjs`.

R04. قلابِ آموزش یک مسئله/منفعت واقعی و شکاف کنجکاوی دارد؛ پاسخ و نام اپ در آغاز آشکار نمی‌شود؛ هر اسلاید یک پیام و مدرک دارد. پایان سؤال مرتبط است. قوانین ۲۱–۳۸.

R05. جستجوی به‌روز و منبع معتبر برای ادعا لازم است. امتیازهای مبتنی بر کلمات در `selection-gate.mjs` اندازه‌گیری احتمال وایرال‌شدن نیستند؛ تاریخ تازهٔ مقاله هم به‌تنهایی اثبات ترند نیست.

R06. رسانه ابتدا واقعی، مرتبط و قابل‌ردیابی باشد؛ ضلع بلند ≥۱۰۸۰ و مساحت ≥۷۰۰۰۰۰ پیکسل؛ نمایش اصلی ≥یک‌سوم قاب. AI فقط پس از شکست جستجوی واقعی همان اسلاید و با برچسب تولیدشده مجاز است؛ اسکرین‌شات یا واقعیت جعلی مجاز نیست. قوانین ۳۹–۵۰ و پیوست دوم.

R07. شکاف اجرای رسانه: `visual-proof.mjs` اندازه و وجود فایل را اجباری می‌کند، ولی شناسنامهٔ کامل، پوشش تصویر و شرح حرکت فقط وقتی `visualEvidence` موجود باشد بررسی می‌شوند؛ منبع مفقود نیز فقط هشدار است. وجود فایل اثبات تطابق معنایی تصویر نیست.

R08. هویت برند ثابت می‌ماند، ولی چیدمان، حرکت، موسیقی و پالت هر محتوا باید دلیل محتوایی داشته باشند. حداکثر سه عنصر فعال؛ متن خوانا و در ناحیهٔ امن؛ قاب گوشی تنها برای UI واقعی. قوانین ۵۱–۶۰. `creative-brief.mjs` اکنون بخشی از انتخاب را با هش تاریخ انجام می‌دهد و عبارت مقایسه با خروجی‌های اخیر به‌تنهایی مدرک اجرای مقایسه نیست.

R09. نریشن پیش‌فرض روشن؛ متن نمایشی و متن گفتاری هم‌معنا ولی جدا. QC روی خودِ فایل صوتی، عدد درست، اتصال عبارت‌ها، نبود هم‌پوشانی و پایان کامل جمله لازم است. ASR به‌تنهایی طبیعی‌بودن لحن را ثابت نمی‌کند. قوانین ۶۱–۷۲ و `VOICE-LOG.md`، `music/voice-qc.mjs`، `lib/voice-timing.mjs`.

R10. آموزش آلمانی زبان مستقل دارد؛ رنگ Artikelها: der آبی، die سرخ، das سبز. واژهٔ آلمانی نباید بی‌قید وارد صدای فارسی شود. کاراکتر کارتونی فعلاً در مسیر درس ممنوع است. شواهد: `german-lesson-build.mjs`، پیوست چهارم.

R11. بانک A1 محدود است؛ با رسیدن به پایان نباید از ابتدا با شمارهٔ جدید تکرار شود. کد اصلی guard دارد؛ تولید درس جدید نیازمند محتوای معتبر و تطابق تعداد آیتم و نریشن است، نه صرفاً افزایش شماره.

R12. retries محدود و علت‌محور باشند؛ قانون فعلی سه تلاش نریشن و حداکثر شش نامزد خودکار دارد. تکرار کور و نامحدود، خروجی بی‌صدا پس از شکست نریشن و حذف گیت ممنوع است. تغییر موضوع خودکار نباید درخواست صریح ادیت کاربر را تبدیل به موضوع دیگر کند.

R13. ارسال تأییدشده و تاریخچه باید ماندگار باشند. `daily.yml` قفل `gapmedia-production` و فایل‌های پیشرفت دارد، ولی کش/JSON/commit جای تراکنش واحد نیست. تاریخچهٔ محلی معادل کل تاریخچهٔ سرور Telegram نیست؛ پوشش ناقص باید اعلام شود.

R14. موتور فعلی Node ESM + HyperFrames 0.8.16 + GSAP + FFmpeg است؛ Remotion و `npm run autopilot` در package.json فعلی موجود نیستند. Worker روی Cloudflare ورودی فرمان را به Actions می‌فرستد؛ رندر داخل Worker نیست.

R15. تعارض‌های اسناد: README-daily قدیمی تأیید دستی می‌خواهد؛ قانون جدید آن را لازم نمی‌داند. متن اصلی قوانین هنوز News دارد؛ پیوست جدید آن را بازنشسته کرده. fallback کم‌تکرارترین موضوع در پیوست دوم نباید ممنوعیت تولید محتوای تکراری را دور بزند. Montserrat و کاراکتر ثابت، درخواست جدیدند نه قانون کشف‌شدهٔ قبلی.

R16. تغییرات محلیِ نهایی‌نشده در بررسی: طراحی evidence-editorial، اصلاح dedupe و حفظ فایل‌های برنامهٔ صوتی. این‌ها را نباید با کد مستقر روی GitHub/Cloudflare یکسان فرض کرد.

## CURRENT_FLOW

1. Telegram → `worker/src/index.js` و وضعیت مکالمه در BOT_STATE → dispatch به `.github/workflows/telegram.yml`؛ مسیرهای محلی `bot.mjs`/`cloud-listen.mjs` نیز وجود دارند و باید مالکیت مصرف پیام‌ها در ممیزی کامل تثبیت شود.
2. زمان‌بندی `.github/workflows/daily.yml` → گیت ساعت برلین + نشانگر نوبت → `daily-render.mjs --only ...`؛ هر فرمت checkpoint مستقل دارد.
3. انتخاب از بانک/تحقیق (`content-search.mjs`، `content-radar.mjs`، `lib/content.mjs`، `lib/features*.mjs`) → بررسی تکرار → تهیه/نجات رسانه → گیت بصری → بریف.
4. `music/plan-voice.mjs` → تبدیل متن گفتاری → TTS → QC → مدت فایل‌ها → طول اسلایدها → `music/make-voice.mjs` → کنترل زمان‌بندی.
5. builder HTML/GSAP → HyperFrames/FFmpeg → فایل نهایی → Telegram → ثبت history/registry/send markers و ذخیره در Git.
6. درس آلمانی: progress → واحد A1 → جستجوی رسانهٔ هر واژه → گفتار فارسی و کلیپ آلمانی → رندر → ارسال → افزایش progress؛ `--unit` مسیر اصلاح است و progress را جلو نمی‌برد.
7. شکست: تشخیص مرحله/گزارش → تلاش محدود یا نامزد جایگزین؛ فایل‌های state متعدد و قراردادهای متفاوت هنوز خطر ناسازگاری دارند.

پوشش ممیزیِ باقی‌مانده: خواندن کامل تمام فایل‌های Markdown، کل Worker، تمام بانک‌های محتوا، باقی مسیرهای جستجو/رندر و تست‌ها؛ تطبیق با HEAD ابری و تنظیمات runtime. تا تکمیل این پوشش، این سند مبنای طرح اولیه است، نه گواهی ممیزی کامل یا مجوز مهاجرت تولید.
