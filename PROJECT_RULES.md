# قانون‌نامهٔ واحد GapMedia و German Insider

> **وضعیت: الزام‌آور.** این فایل مرجع نهاییِ تولید، پژوهش، رندر، ارسال و فرمان‌های بات است. هر سند یا کد قدیمی که با آن ناسازگار باشد باید اصلاح شود؛ تا زمان اصلاح، این فایل اولویت دارد.

## ۱. دامنه و برند

1. آموزش‌ها با برند GapMedia ساخته می‌شوند.
2. خبرها با هویت German Insider ساخته می‌شوند.
3. خبرها نام، لوگو یا CTA مربوط به GapMedia ندارند.
4. انتخاب موضوع به TikTok و Instagram محدود نیست.
5. TikTok و Instagram مقصد انتشارند، نه مرز انتخاب محتوا.

## ۲. انتخاب موضوع

6. هر موضوع باید ترند واقعی، نیاز واقعی مخاطب، یا مسیر واقعیِ صرفه‌جویی در زمان، مهارت یا درآمدِ ممکن داشته باشد.
7. موضوع بدون منفعت روشن ساخته نمی‌شود.
8. «جدید»، «خفن» یا «رایگان» به‌تنهایی دلیل ساخت نیست.
9. درآمد، View و Viral تضمین نمی‌شوند و فقط با منبع معتبر مطرح می‌شوند.
10. خروجی‌های قبلی تلگرام پیش از انتخاب موضوع بررسی می‌شوند.
11. منابع زمان‌حساس باید معتبر و تاریخ‌دار باشند.
12. اپ‌ها باید منبع رسمی، App Store، Google Play یا سایت سازنده داشته باشند.
13. هر اپ یا قابلیت باید کاربرد دقیق و قابل‌نمایش داشته باشد.
14. موضوع بی‌منبع یا بی‌مدرک رد و با جستجوی تازه جایگزین می‌شود.
14-الف. پیش از هر ساخت، تاریخچهٔ کامل بات، فهرست خروجی‌های تلگرام و بانک محتوای ساخته‌شده بررسی می‌شود؛ عنوان، موضوع، اپ یا قابلیت، قلاب و ساختار با خروجی‌های قبلی مقایسه می‌شوند.
14-ب. موضوع، اپ، قابلیت، قلاب یا ساختارِ تکراری ساخته نمی‌شود؛ در صورت شباهت معنادار، سیستم خودکار گزینهٔ تازه و مرتبط پیدا می‌کند.
14-ب-۱. پیش از هر رندر، شناسه، موضوع، قلاب و ساختار با کل تاریخچهٔ خروجی‌های موفق مقایسه می‌شود؛ گیت فقط به ۳۰ روز یا یک فایل کش محدود نیست. تطابق قطعی ساخت را متوقف و نامزد تازه را جایگزین می‌کند.
14-ب-۲. ساخت زمان‌بندی‌شده و ساخت از فرمان تلگرام قفل مشترک تولید دارند؛ هیچ‌کدام حق ندارد با تاریخچهٔ قدیمی و هم‌زمان با دیگری رندر را آغاز کند.
14-ج. «Kling AI» در تاریخچهٔ این پروژه ساخته شده و تا زمانی که کاربر صریحاً موضوع تازه و متفاوتی از Kling را درخواست نکرده، نامزد ساخت نیست.
14-د. نبودن رسانهٔ یک منبع، پایان کار نیست: ابتدا سایت رسمی، صفحهٔ محصول، مستندات، فروشگاه رسمی و رسانه‌های معتبر برای تصویر یا ویدیوی واقعی همان قابلیت بررسی می‌شوند.
14-ه. اگر رسانهٔ واقعی همان قابلیت پیدا نشد، فقط بخشی از همان اپ که رسانهٔ واقعی دارد جایگزین می‌شود؛ اگر آن هم ممکن نبود، موضوع به اپ یا قابلیت جدید و تأییدشده تغییر می‌کند.
14-و. رابط حدسی، لوگوی ساختگی، آیکن غیرواقعی، اسکرین‌شات جعلی و تصویرسازی عمومی هرگز جای رسانهٔ واقعی را نمی‌گیرند.
14-ز. پیش از انتشار، غلط املایی، تکرار، لوگوی اشتباه، تصویر نامرتبط، ادعای نادرست و کیفیت رسانه دوباره بررسی می‌شوند.

## ۳. ساخت خودکار

15. تصویر، ویدیو و ساخت، تأیید دستی نمی‌خواهند.
16. فقط موضوعی که تمام گیت‌های کیفیت را پاس کند خودکار ساخته و ارسال می‌شود.
17. شکست حتی یک گیت، ساخت را متوقف می‌کند.
18. دلیل رد باید دقیق ثبت شود و سیستم منبع یا موضوع جایگزین پیدا کند.
19. فقط گزینه‌های هم‌امتیاز و معتبر به‌صورت شماره‌دار برای انتخاب فرستاده می‌شوند.
20. متن، عکس یا ویدیوی مستقیمِ کاربر پس از پاس گیت کیفیت، بی‌نیاز از تأیید دوباره ساخته می‌شود.

## ۴. قلاب

21. قلاب در سه ثانیهٔ اول باید اسکرول را متوقف کند.
22. قلاب یک درد آشنا، منفعت ملموس، تضاد، اشتباه رایج یا نتیجهٔ قابل‌وفا را نشان می‌دهد.
23. نام اپ، سایت، قابلیت، برند یا جواب اصلی در قلاب نمی‌آید.
24. پاسخ یا مسیر کامل در قلاب افشا نمی‌شود.
25. خط دوم فقط حلقهٔ کنجکاوی کوتاه است.
26. قلاب گنگ، کلیشه‌ای، فریبنده یا اغراق‌آمیز ممنوع است.
27. آموزش با قوی‌ترینِ ادعا، تضاد یا سؤال آغاز می‌شود.
28. خبر با تیتر اثرگذار و دقیق آغاز می‌شود، نه سؤال.
29. «رایگان» به‌تنهایی در قلاب نمی‌آید.
30. «رایگان» فقط همراه سرویس و منفعتِ تأییدشده نوشته می‌شود.

## ۵. ساختار محتوا

31. هر ویدیو یک ایدهٔ اصلی دارد.
32. هر اسلاید یک عمل، یک پیام و یک مدرک بصری دارد.
33. آموزش‌ها مرحله‌به‌مرحله، کوتاه و قابل‌اجرا هستند.
34. نام انگلیسی و مسیر واقعی اپ پس از قلاب نمایش داده می‌شود.
35. واژه‌های شناخته‌شده می‌توانند فارسی و انگلیسی کنار هم باشند: Edit، Hook، View، Save و Search.
36. پایان ویدیو سؤال واقعی برای گفت‌وگو دارد.
37. Like یا Follow، CTA اصلی نیستند.
38. درخواست یک کلمه در کامنت فقط در برابر لینک، فایل یا راهنمای واقعی مجاز است.

## ۶. تصویر و ویدیو

39. هر اسلاید تصویر یا ویدیوی واقعیِ همان موضوع دارد. **استثنا (تصمیم صاحب کانال،
    ۲۰۲۶-۰۹-۱۱، پیوست دوم):** برای هر اسلاید ابتدا باید یک جستجوی واقعی انجام
    شود؛ فقط اگر آن جستجو برای همان اسلاید چیزی پیدا نکرد، یک تصویر تولیدشده با
    هوش مصنوعی («ai-generated» در `visualEvidence.sourceType`) جایگزین می‌شود.
    این استثنا فقط برای «سلایدی که واقعاً عکس واقعی ندارد» است، نه جایگزین
    جستجوی واقعی.
40. منبع رسانه رسمی، معتبر یا ارسال‌شده توسط صاحب کانال است؛ یا — فقط طبق
    استثنای رول ۳۹ — تولیدشده با هوش مصنوعی و علامت‌گذاری‌شده به همین عنوان.
41. رابط حدسی، آیکن عمومی، لوگوی تنها، نمودار تزئینی و تصویر نامرتبط ممنوع است.
42. ضلع بلند رسانه دست‌کم ۱۰۸۰ پیکسل و مساحت آن دست‌کم ۷۰۰٬۰۰۰ پیکسل است.
43. تصویر اصلی دست‌کم یک‌سوم قاب عمودی را می‌گیرد.
44. تصویر بدون متن هم باید مفهوم اسلاید را برساند.
45. تکرار رسانه فقط با بخش، عمل و برش واقعاً متفاوت مجاز است.
46. قاب موبایل فقط برای یک صفحهٔ واقعی و خوانای اپ مجاز است.
47. دو صفحهٔ کوچک موبایل در یک اسلاید ممنوع است.
48. Save فقط Bookmark واقعی، Share فقط کنترل واقعیِ Share و Analytics فقط مدرک واقعیِ Insights دارد.
49. تصویر قلاب باید معنی وعده را نشان دهد، نه نام یا لوگوی اپ.
50. معرفی اپ یا سرویس پس از قلاب، لوگوی واقعی با رنگ اصلی آن را دارد.

## ۷. گرافیک و موشن

51. هر ویدیو هویت بصری مستقل دارد.
52. پالت، فونت، چیدمان، ترنزیشن، موشن و موسیقی از موضوع همان ویدیو می‌آیند.
53. TikTok و Instagram با تغییر رنگِ یک قالب مشترک ساخته نمی‌شوند.
54. هر اسلاید حرکت اصلی، واکنش ثانویه و حرکت محیطی آرام دارد.
55. بیش از سه عنصر متحرک هم‌زمان مجاز نیست.
56. حرکت اصلی عمل همان اسلاید را نشان می‌دهد.
57. ترنزیشن با معنی اسلاید مرتبط است.
58. لرزش بی‌دلیل، Whip تصادفی و افکت تزئینی بی‌ربط ممنوع است.
59. متن در ناحیهٔ امن عمودی و دور از کنترل‌های پلتفرم است.
60. پس‌زمینهٔ آموزشی طبیعی و متناسب با موضوع است؛ نارنجی/سرخِ تکراری ممنوع است.
60-الف. زبان بصری پایهٔ پروژه‌ها از هم جداست: TikTok با کات‌های لمسی و کنتراست پویای Cutout، Instagram با چیدمان ادیتوریال و قابِ عکس‌محور، معرفی اپ با Product Lab و مدرکِ محصول، و درس آلمانی با Flashcard واقعیِ واژه و عکس ساخته می‌شود. این‌ها فقط نقطهٔ شروع‌اند؛ هر موضوع باید تصویر اصلی، استعاره، حرکت و گذار ویژهٔ خود را داشته باشد، نه بازتولید یک صحنه با رنگ دیگر.

## ۸. نریشن فارسی

61. صدا پیش‌فرض همهٔ ویدیوهاست؛ فقط «بدون صدا» آن را خاموش می‌کند.
62. نریشن فارسی معیار، طبیعی، پرانرژی و مناسب مخاطب افغانستان است.
63. متن تصویر و متن گفتار جدا ولی هم‌معنا هستند.
64. خواندن کلمه‌به‌کلمه، رباتی، کشیده یا بسیار رسمی ممنوع است.
65. مکث فقط در پایان فکر، جمله یا نشانهٔ نگارشی واقعی می‌آید.
66. میان نام چندبخشی، فعل مرکب، اضافه، حرف اضافه و متمم مکث ممنوع است.
67. URL، هشتگ، HTML، پرانتز، شماره‌گذاری و دستور اجرایی خوانده نمی‌شوند.
68. پایان «ی»، «ه» و واژه‌هایی مانند «می‌خواهید» کشیده نمی‌شوند.
69. قلاب با انرژی طبیعی و سرعت عادی خوانده می‌شود.
70. جمله پیش از تغییر اسلاید تمام می‌شود.
71. صدای دو جمله روی هم نمی‌افتد.
72. هر ایراد تلفظ با زمان، علت، اصلاح و نمونهٔ شنیداری در `VOICE-LOG.md` ثبت می‌شود.
72-الف. هر خط نریشن پیش از رندر با متن گفتاریِ جداگانه، بدون رقم و با قواعد استاندارد فارسی آماده می‌شود؛ متنِ روی اسلاید هرگز مستقیماً ورودی موتور صدا نیست.
72-ب. هر کلیپِ صوتیِ واقعی پیش از ساخت ویدیو با ASR فارسی و مقایسهٔ آوایی با همان متن گفتاری بررسی می‌شود. هجا یا واژهٔ اضافه، واژهٔ حذف‌شده، عدد یا تلفظِ تغییرکرده رد قطعی است.
72-پ. در صورت رد ASR، فقط یک بازتولید تازه با همان متن مجاز است. ردِ دوباره، خروجی را متوقف می‌کند؛ ارسال موسیقی‌تنها یا نریشنِ تأییدنشده ممنوع است.
72-ت. برای عددهایی که موتور در آن‌ها سابقهٔ خطا دارد، متن گفتاری باید کوتاه‌واکهٔ لازم را داشته باشد؛ نمونهٔ ثابت: «صِفر». متن نمایش همچنان شکل معیار «صفر» را نگه می‌دارد.
72-ث. گزارش هر گیت شامل متن ارسالی، متن شنیده‌شدهٔ ASR، نتیجه، شناسهٔ صدا و زمان بررسی است. ASR جای شنیدن انسان را نمی‌گیرد؛ ایراد شنیداریِ گزارش‌شده همیشه یک باگ انتشار است و پیش از خروجی بعدی باید اصلاح شود.
72-ج. برچسب‌های واقعیِ رابط کاربری روی تصویر انگلیسی می‌مانند، اما فقط متن گفتاریِ خصوصی به معادل طبیعی و آزمودهٔ فارسی تبدیل می‌شود. واژه‌های گزارش‌شدهٔ ناامن مانند `Preview`، `Microphone` و «میکروفون» نباید خام به موتور TTS برسند؛ عبور خام آن‌ها گیت نریشن را رد می‌کند.
72-چ. کلید کش هر فایل نریشن باید شامل هشِ متن گفتاریِ دقیق باشد. هر اصلاح تلفظ، اجباراً صوت تازه و کنترل ASR تازه می‌گیرد؛ استفاده از MP3 قدیمی پس از تغییر متن گفتاری ممنوع است.

## ۹. موسیقی

73. موسیقی با موضوع، سبک بصری و ریتم اسلایدها هماهنگ است.
74. ضرب تغییر اسلاید با حرکت اصلی همان اسلاید هماهنگ است.
75. موسیقی هنگام نریشن Duck می‌شود و صدا را نمی‌پوشاند.
76. آغاز موسیقی انرژی کافی برای توقف اسکرول دارد.
77. موسیقی در میانه قطع یا تا پایان بی‌انرژی نمی‌شود.
78. TikTok و Instagram می‌توانند برای یک موضوع موسیقی متفاوت داشته باشند.

## ۱۰. خبر German Insider

79. خبر از آموزش GapMedia جداست.
80. خبر دربارهٔ آلمان، اروپا، مهاجرت، افغان‌ها یا اثر واقعی بر مهاجران است.
81. منبع خبر معتبر و تاریخ‌دار است.
82. منبع در خود ویدیو فقط با درخواست کاربر گفته می‌شود.
83. خبر کوتاه، دقیق، بی‌طرف و بدون شایعه است.
84. هر خبر رسانهٔ واقعی و مرتبط دارد.
85. خبرِ ارسال‌شده دوباره ساخته نمی‌شود.
86. طول خبر فعلاً ۲۰ تا ۳۰ ثانیه است.

## ۱۱. بات تلگرام

87. همهٔ فرمان‌ها از یک موتور فرمان مشترک اجرا می‌شوند.
88. بات محلی، Worker و GitHub برای یک فرمان پاسخ متفاوت نمی‌دهند.
89. جستجوی محتوا و خبر زنده است و نتیجهٔ خارجی فارسی می‌شود.
90. نتایج با شماره نمایش داده می‌شوند.
91. انتخاب با شماره، `موضوع ۱` یا `منبع ۲` کار می‌کند.
92. بات ادیت قلاب، اسلاید، موضوع، صدا و ساخت از متن/عکس/ویدیو را پشتیبانی می‌کند.
93. ساخت مستقیم تنها پس از پاس گیت کیفیت اجرا می‌شود.
94. بات پیش از تأیید واقعیِ رندر و ارسال، موفقیت اعلام نمی‌کند.

## ۱۲. زمان‌بندی و خروجی

95. تولید روزانه ساعت ۰۸:۳۰ و ۱۷:۰۰ به وقت آلمان است.
96. هر نوبت دو نسخهٔ بومی، یکی TikTok و یکی Instagram، می‌سازد.
97. دو نسخهٔ یک موضوع قلاب، گرافیک، موشن، موسیقی و CTA متفاوت دارند.
98. خروجی عمودی ۱۰۸۰×۱۹۲۰ است؛ 4K فقط با دستور مشخص.
99. تصویر، متن، صدا، موسیقی، ناحیهٔ امن و تکراری‌نبودن پیش از ارسال بررسی می‌شوند.
100. فقط خروجی پاس‌شده به تلگرام ارسال می‌شود.

## ۱۳. گیت انتشار

101. حداقل امتیاز انتشار ۹۰ از ۱۰۰ است.
102. تقاضا و منفعت: ۲۵ امتیاز.
103. صحت منبع: ۲۰ امتیاز.
104. قدرت قلاب: ۱۵ امتیاز.
105. مدرک واقعی تصویر یا ویدیو: ۱۵ امتیاز.
106. خوانایی و موشن: ۱۰ امتیاز.
107. نریشن: ۱۰ امتیاز.
108. تمایز از خروجی‌های اخیر: ۵ امتیاز.
109. شکست در منبع، رسانهٔ واقعی، نریشن، تکراری‌بودن یا ادعای بی‌اساس، بدون توجه به امتیاز کل، مانع انتشار است.
110. گزارش رد، اسلاید، دلیل و راه اصلاح را دقیق مشخص می‌کند.

## پیوست: وضعیت پیاده‌سازی (Claude، ۲۰۲۶-۰۹-۰۶)

این بخش را من (Claude) اضافه کردم تا مشخص باشد کدام رول با کد فعلی اجرا
می‌شود و کجا کامل نیست — بخشی از خودِ قانون‌نامه نیست، فقط ردیابی است.

- **رول ۱۵/۲۰ (بدون تأیید دستی):** تأیید کاربر گرفته شد (۲۰۲۶-۰۹-۰۶) —
  دیگر برای هیچ ویدیویی که گیت‌های کد را پاس کند، تأیید جداگانه در تلگرام
  یا در چت گرفته نمی‌شود.
- **رول ۱۷/۱۸ (رد گیت → ثبت دلیل + جایگزین):** `daily-render.mjs` — وقتی
  `assertVisualProof(pack)` یک اسلات را رد کند، رواند رتری تا ۶۰ بار
  قابلیت بعدی همان بانک را امتحان می‌کند (`avoidIds` +
  `dailyDeliveriesForDate(date, avoidIds)`، کامیت `83789c5`) تا یک
  جایگزین پیدا کند؛ اگر هیچ‌کدام پاس نشد، آن اسلات رد می‌شود، دلیل دقیق
  در کنسول و در یک پیام تلگرام فارسی ثبت می‌شود، و اجرا به‌جای کرش با
  `exit 0` تمام می‌شود (کامیت `6508cfd`).
- **رول ۳۹–۵۰ / ۱۰۹ (مدرک تصویری واقعی):** `lib/visual-proof.mjs` —
  `isRealAsset`، اندازهٔ تصویر (`imageSize` ≥ ۱۰۸۰px و ≥ ۷۰۰٬۰۰۰px) و
  چک برش/تمرکز متفاوت همیشه اجباری‌اند، بدون استثنا. شناسنامهٔ کامل
  `visualEvidence` (سورس، coverage، حرکت) فقط وقتی اسلاید آن را دارد
  بررسی می‌شود — چون فقط ۳ قابلیت از ۶۰ تای بانک فعلی این ساختار را
  دارند؛ اجباری‌کردن بدون استثنا کرش تقریباً هر ساخت عادی را باعث می‌شد
  (دیدهٔ زنده ۲۰۲۶-۰۹-۰۵: ۵۷ از ۵۹ رد می‌شدند).
- **رول ۱۱۰ (راه اصلاح در گزارش رد):** ناقص — پیام رد فعلاً شمارهٔ
  اسلاید و دلیل را می‌دهد (پیام خطای خودِ `assertVisualProof`)، ولی
  «راه اصلاح» را جدا و صریح نمی‌نویسد.

### پیوست دوم (Claude، ۲۰۲۶-۰۹-۱۱) — گزارش کاربر: نشر تکراری، عکس تکراری، موضوع تکراری

سه باگ واقعی که صاحب کانال زنده گزارش داد و در همین تاریخ اصلاح شد. ثبت شده
اینجا (نه فقط در حافظهٔ این نشست) چون صاحب کانال از چند Agent مختلف استفاده
می‌کند — هر Agent باید این‌ها را قبل از هر ساخت بداند.

- **نشر تکراری (آموزش آلمانی A1-010 دوباره ارسال شد):** علت واقعی —
  `news-scan.yml` و `daily.yml` هر دو، بعد از هر ارسال موفق، پیشرفت/حافظهٔ
  ارسال را با `git push || true` ذخیره می‌کردند؛ شکست پوش (رقابت با پوش
  یک ورک‌فلو دیگر روی همان `main`) کاملاً بی‌صدا نادیده گرفته می‌شد و Job
  سبز گزارش می‌شد، در حالی که پیشرفت واقعاً ذخیره نشده بود — اجرای بعدی
  همان قسمت/محتوا را دوباره می‌ساخت و می‌فرستاد. **اصلاح:** هر دو فایل حالا
  یک بار retry (`git pull --rebase` + push دوباره) می‌کنند و اگر باز هم
  شکست بخورد، Job را قرمز می‌کنند و یک پیام هشدار به تلگرام می‌فرستند —
  دیگر بی‌صدا رد نمی‌شود. اگر باز هم چنین گزارشی آمد، این مکانیزم را اول
  بررسی کن، نه فقط منطق انتخاب محتوا را.
- **یک عکس واقعی در همهٔ اسلایدهای تیک‌تاک/انستا تکرار می‌شد:**
  `lib/auto-image.mjs`'s `rescuePackPhotos()` قبلاً فقط یک‌بار جستجو
  می‌کرد و همان یک عکس را با crop/focus متفاوت روی همهٔ اسلایدهای بی‌عکس
  می‌گذاشت. **اصلاح:** حالا برای هر اسلاید یک جستجوی واقعیِ جدا انجام
  می‌شود (دقیقاً همان الگویی که `lib/lesson-image.mjs` برای سری آلمانی
  از قبل استفاده می‌کرد)، و عکس‌های قبلاً استفاده‌شده در همان ساخت
  حذف/رد می‌شوند تا اسلاید بعدی روی عکس تکراری نیفتد.
  **درخواست اولیه رد شد، بعد صاحب کانال صریحاً قانون را عوض کرد
  (۲۰۲۶-۰۹-۱۱):** اول درخواست شد اگر عکس واقعی برای یک اسلاید پیدا نشد،
  «تصویر حدسی» (AI) جایگزین شود؛ طبق رول ۳۹/۴۱ وقت آن **اجرا نشد**. صاحب
  کانال با دیدن دلیل، صریحاً خواست خودِ قانون ۳۹/۴۰ عوض شود: «اول عکس
  واقعی، بعدش اگر اسلاید بعدی خالی بود، عکس هوش مصنوعی شبیه متن — نه عکس
  واقعی یا حدسی تکراری». رول ۳۹/۴۰ بالا رسماً همین استثنا را می‌گویند و
  `lib/auto-image.mjs`'s `generateAIImage()` همین را پیاده کرده: برای هر
  اسلاید اول `findRealImage()` واقعی اجرا می‌شود؛ فقط اگر آن برای همان
  اسلاید چیزی پیدا نکرد، یک تصویر تولیدشده جای آن می‌نشیند —
  `visualEvidence.sourceType: "ai-generated"` (در `lib/visual-proof.mjs`'s
  `VALID_SOURCE_TYPES` اضافه شد) تا همیشه از عکس واقعی قابل‌تشخیص بماند.
  هیچ عکسی (واقعی یا تولیدشده) بین اسلایدها تکرار نمی‌شود.
- **تیک‌تاک/انستا مدام روی همان موضوع می‌ماند (رول ۱۴-الف/ب نقض می‌شد):**
  علت واقعی — `lib/features.mjs`'s `featureFor()`/`aiFeatureFor()` وقتی
  همهٔ نامزدهای یک دسته «قبلاً منتشرشده» بودند، به‌جای رد یا جستجوی
  واقعی، بی‌صدا به `list[i]` (همان انتخاب خام روزانه، بدون توجه به
  `exclude`) برمی‌گشت — یعنی وقتی دستهٔ کوچکی مثل تیک‌تاک (بعد از گیت
  واقعیِ عکس) خیلی زود ته می‌کشید، همان شناسه بارها پشت‌سرهم انتخاب
  می‌شد. **اصلاح:** یک fallback جدید — وقتی همه چیز exclude شده، به‌جای
  `list[i]`، شناسه‌ای انتخاب می‌شود که **دیرترین بار** منتشر شده (یا اصلاً
  منتشر نشده)، بر پایهٔ `.content-history.json` (`lib/content.mjs`'s
  `publishedAtById()` → `lib/features.mjs`'s `leastRecentlyUsed()`).

### پیوست سوم (Claude، ۲۰۲۶-۰۹-۱۱) — نشر تکراریِ آموزش آلمانی: علت دومِ کاملاً جدا

بعد از اصلاح «پیوست دوم» (رقابت پوش گیت‌هاب)، صاحب کانال دوباره یک تکرار واقعی
در آموزش آلمانی گزارش داد (قسمت A1-011 دقیقاً محتوای A1-003 را دوباره تدریس
کرد). علت این‌بار کاملاً جدا بود — هر دو باید همیشه چک شوند.

- **علت واقعی:** `lib/german-a1.mjs`'s `GERMAN_A1` فقط ۸ واحد داشت.
  `germanUnitAt(index)` با `index % GERMAN_A1.length` هر شمارهٔ ایندکس را
  wrap می‌کرد — یعنی به‌محض رسیدن `nextIndex` به عدد ۸، دوباره از واحد اول
  شروع می‌کرد، اما با یک شمارهٔ قسمتِ تازه (A1-009, A1-010, ...) — محتوای
  آلمانی/فارسی عیناً همان بود، فقط شمارهٔ روی صفحه فرق داشت. `check(print)`
  (دوپلیکیت‌چک در `german-lesson-build.mjs`) این را نگرفت.
- **اصلاح:**
  1. `GERMAN_A1` به ۲۴ واحد رسید (a1-01 تا a1-24) — فاصلهٔ زیادی تا برخورد
     دوباره به این دیوار.
  2. `german-lesson-build.mjs` حالا قبل از هر کاری چک می‌کند
     `idx >= GERMAN_A1.length`؛ اگر بانک تمام شده باشد، **رد می‌شود** (پیام
     تلگرام + `exit 1`) به‌جای wrap بی‌صدا. اگر باز چنین خطایی آمد، یعنی
     واحدهای بیشتری باید به `lib/german-a1.mjs` اضافه شود — رفع‌کردنش
     همیشه یعنی «محتوای تازهٔ واقعی اضافه کن»، نه اینکه گیت را حذف کنی.
  3. هر واحد تازه باید هم در `lib/german-a1.mjs` (کلمه/عبارت آلمانی، معنی
     فارسی، عبارت جستجوی عکس، مثال) **و هم** در `lib/narration.mjs`'s `VO`
     (hook + یک step فارسیِ طبیعی به ازای هر آیتم + outro) اضافه شود — اگر
     `VO[id]` نباشد یا طول `steps` با طول `items` جور نباشد، ساخت با صدا
     می‌شکند. یک اسکریپت سریع برای چک این تطبیق:
     `node -e 'import("./lib/german-a1.mjs").then(async({GERMAN_A1})=>{const{narrationFor}=await import("./lib/narration.mjs");for(const u of GERMAN_A1){const vo=narrationFor(u.id);if(!vo||vo.steps.length!==u.items.length)console.log("BAD",u.id)}console.log("done")})'`
  4. `.gitignore` به `compositions/german/` هم اضافه شد (مثل
     `compositions/daily/`/`compositions/news/` که قبلاً داشتند) — فایل‌های
     مرحلهٔ ساخت نباید بعد از ارسال باقی بمانند.
- **درخواست تقویت محتوا:** صاحب کانال خواست «زمان و خود محتوا بیشتر شود».
  واحدهای تازه اکثراً با ۴ تا ۵ آیتم نوشته شدند (برخی از قبلی‌ها بیشتر).
  چون مدت واقعیِ هر قسمت از روی صدای ساخته‌شده تعیین می‌شود (نه یک عدد
  ثابت)، بزرگ‌ترین اهرم واقعی برای «طولانی‌تر و پرمحتواتر» همین «چند آیتم
  در هر واحد» است، نه تغییر عددهای HOOK_DUR/TIP_DUR (که فقط fallback
  بدون‌صدا هستند).

### پیوست چهارم (Claude، ۲۰۲۶-۰۹-۱۱) — قاعدهٔ ثابت برای lib/narration.mjs: کلمهٔ آلمانی را در متن فارسی ننویس

بعد از دو اصلاحِ درستِ دیگر روی صدای کلمهٔ آلمانیِ قسمت A1-012 (هم رگرسیون
پیچ/سرعت، هم سرعت/بلندیِ خودِ کلیپ آلمانی)، صاحب کانال باز هم گزارش داد
تلفظ اشتباه است — این‌بار مشخص کرد: «sein» به‌جای «زاین» (تلفظ درست آلمانی،
s ابتدای کلمه پیش از مصوت = z) شبیه «ساین» (انگلیسی/فارسی) شنیده می‌شد.

**علت واقعی، کاملاً جدا از دو اصلاح قبلی:** ورودی `a1-12-haben` در
`lib/narration.mjs` کلمهٔ آلمانیِ «sein» را با حروف لاتین مستقیماً داخل
متن نریشنِ **فارسی** (hook و outro) نوشته بود — همان متنی که با صدای
فارسی (بدون `language_boost="German"`) خوانده می‌شود. ربطی به
`GERMAN_WORD_VOICE_ID`/`GERMAN_WORD_VOICE_SETTINGS` ندارد؛ آن‌ها فقط روی
کلیپ جداگانهٔ کلمهٔ آلمانی اعمال می‌شوند، نه روی hook/outro.

**اصلاح:** هر ۲۴ واحد `lib/narration.mjs` چک شد — فقط همین یکی کلمهٔ
آلمانی را داخل متن فارسی نوشته بود؛ به «فعل قبلی» تغییر کرد.

**قاعدهٔ دائمی برای هر ورودیِ تازهٔ `lib/narration.mjs` (فارسی یا هر پکِ
دیگر با کلمهٔ خارجی):** هرگز یک کلمهٔ آلمانی (یا هر زبان دیگر) را با حروف
لاتین داخل hook/step/outro ننویس — این‌ها همیشه با صدای فارسیِ بدون
override زبان خوانده می‌شوند. اگر لازم بود به کلمهٔ واحد دیگری اشاره شود،
توصیفش کن («فعل قبلی»، «کلمهٔ قبلی»)، هجی‌اش نکن.

**یادداشت جانبی، دست‌نخورده ماند:** خط ۱۱۵ همین فایل (`a1-01-greetings`)
هم کلمهٔ «Guten» را داخل یک نکتهٔ گرامریِ فارسی دارد — قدیمی است، بخشی از
قسمت ۹ بود که صاحب کانال زنده تأیید کرده («ویدیو را دیدم درست بود»). چون
هیچ گزارش جدیدی دربارهٔ آن نیامده و تغییر محتوای LOCKED بدون شنیدن واقعیِ
تازه خلاف قاعدهٔ همین پروژه است، دست نخورد — فقط اینجا ثبت شد که اگر روزی
گزارش مشابهی دربارهٔ آن آمد، این همان الگوست.

### پیوست پنجم (Claude، ۲۰۲۶-۰۹-۱۱) — حذف کامل خط تولید News + رفع باگ‌های گرافیکیِ ناشی از قرض‌گرفتنِ platform="news" توسط سری آلمانی

صاحب کانال دستور داد: «News دیگر نمی‌سازیم پس حذفش کن.» بررسی نشان داد خط
تولید خبر (کانال قدیمیِ German Insider) از ۲۰۲۶-۰۹-۰۸ عملاً غیرفعال بود —
`news-scan.yml` کامل به سری آموزش آلمانی تبدیل شده بود — اما ردپای آن در
۱۰ فایل باقی مانده بود، از جمله یک مسیر کد کاملاً مرده در
`daily-render.mjs` (`isNewsRun`، پرچم `--news-generated` که فایلی ناموجود
import می‌کرد) و منطق `category === "news"` در `lib/content.mjs`.

**مهم‌تر:** سری آموزش آلمانی برای گرفتن چیدمان بدون‌برند، از همان
`platform: "news"` «قرض» گرفته بود — و همین قرض‌گرفتن، بدون آنکه کسی متوجه
شود، باعث ۴ باگ واقعی شده بود:

1. **گیت کیفیت تصویر (`assertVisualProof`) کامل غیرفعال بود** — چون تابع
   با `if (pack.platform === "news") return;` برای هر قسمتِ آلمانی زودتر
   خارج می‌شد؛ هیچ‌وقت چک فایل واقعی/اندازه/تکرار روی این ویدیوها اجرا
   نشده بود.
2. **عکس هوک همان عکس اولین اسلاید واژگان بود** — چون `pack.hookPhoto`
   هیچ‌وقت ست نمی‌شد، `build-ink.mjs` به‌صورت خودکار اولین `tip.photo` را
   برای هوک هم برمی‌داشت؛ دقیقاً الگوی ممنوعِ «تصویر تکراری بین اسلایدها»ی
   `VISUAL_QC_STANDARD.md`.
3. **شمارندهٔ «A1 • 0XX/100» فقط روی هوک دیده می‌شد**، نه روی اسلایدهای
   واژگان — چون رندر `.kick` در سناریوهای step با شرط
   `pack.platform === "news"` خاموش می‌شد.
4. **متنِ CTA پایانی اشتباه بود** — هر قسمت آلمانی در پایان می‌گفت «برای
   خبرهای بعدی، ما را دنبال کن» (متنِ کانال خبری، نه آموزشی) چون این خط
   هم روی `platform === "news"` شرط شده بود.

**اصلاح:** پک آموزش آلمانی حالا `platform: "german-lesson"` دارد (به‌جای
قرض‌گرفتنِ «news») و هر کمکی که واقعاً برای سری آلمانی لازم بود
(`lib/brand.mjs`، `lib/scene-art.mjs`، خطوط بالا در `build-ink.mjs`) روی
`pack.kind === "german-lesson"` کلید خورد، نه platform. مسیرهای کد مردهٔ
خبر (`daily-render.mjs`، `lib/content.mjs`، `lib/creative-brief.mjs`،
`music/mood.mjs`) حذف شد. علاوه بر این، کوئری عکسِ ۲ آیتم از واحد
`a1-12-haben` (که همان قسمتی‌ست که ۳ بار قبلاً اصلاح صدا خورد) اصلاح شد:
«person holding coffee cup» به‌جای «Zeit» (وقت) و «person holding phone
smiling» به‌جای «Auto» (ماشین) — هیچ‌ربطی به جملهٔ مثالِ خودشان نداشتند.

هر ۶ اصلاح با یک آزمایش مصنوعی (بدون نیاز به کلیدهای API که در این محیط
موجود نیست) روی خروجی واقعیِ `buildInkHTML`/`assertVisualProof` تأیید شد،
و `npm test` کامل (۱۴ فایل تست) بدون هیچ رگرسیونی پاس کرد. هنوز رندر واقعی
(با صدا و عکسِ واقعیِ شبکه) روی GitHub Actions تأیید نشده — تا وقتی صاحب
کانال یک قسمت تازه را گوش/تماشا نکرده، این تغییرات NOT LOCKED است.
